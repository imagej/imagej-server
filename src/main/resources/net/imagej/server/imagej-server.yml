# Logging settings.
logging:
  level: INFO
  appenders:
    - type: console
      threshold: ALL
      timeZone: UTC
      target: stdout
      logFormat: # TODO

# Disable HTTP access logging (logback-access 1.3.x is incompatible with logback-core 1.3.x).
server:
  requestLog:
    appenders: []
